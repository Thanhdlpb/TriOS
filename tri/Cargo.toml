[package]
name = "tri"
version = "0.1.0"
edition = "2021"
description = "TriOS CLI – Bootstrap tool & REPL"

[dependencies]
clap = { version = "4", features = ["derive"] }
tricore = { path = "../tricore" }
